<?xml version="1.0" encoding="utf-8"?>
<!--
=============================================================================
File:           mobile/android/core/location/src/main/AndroidManifest.xml
Project:        Qervon
Author:         USDTG GROUP TECHNOLOGY LLC
Developer:      Irfan Gedik
Created Date:   2026-08-12
Version:        0.1.0

Description:
  Declares the permissions and the foreground Service this module owns.
  Merged into both app-courier's and app-customer's manifests at build
  time (only app-courier actually starts the service).

License:
  Qervon License v1.0 — see LICENSE in the repository root.
=============================================================================
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!--
    Required on Android 10+ (API 29+) for FusedLocationProviderClient to keep
    delivering updates once the app is no longer the foreground UI (screen
    off, another app on top) — a foreground service alone is not always
    sufficient on every OEM skin. Must be requested as a separate runtime
    permission request *after* ACCESS_FINE_LOCATION is granted; the OS
    silently denies it if bundled into the same request (see
    CourierApp.kt's two-step permission flow).
    -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application>
        <service
            android:name=".CourierLocationService"
            android:exported="false"
            android:foregroundServiceType="location" />
    </application>
</manifest>
